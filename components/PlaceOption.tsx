import { View, Text } from "react-native";
import React from "react";

type Props = {};

const PlaceOption = (props: Props) => {
	return (
		<View>
			<Text>PlaceOption</Text>
		</View>
	);
};

export default PlaceOption;
