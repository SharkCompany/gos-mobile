import { View, Text } from "react-native";
import React from "react";

type Props = {};

const CreateRide = (props: Props) => {
	return (
		<View>
			<Text>CreateRide</Text>
		</View>
	);
};

export default CreateRide;
